# AGENTS.md

## Project Overview
本專案是「校園學餐等待時間規劃系統」的前端展示 Demo。
目標是做出一個可以在瀏覽器中打開的靜態網站，展示：
- 校園學餐餐廳節點與位置
- 各餐廳即時預估等待時間
- 依據等待時間排序與篩選功能
- 起始位置到目標餐廳的複合時間（步行 + 排隊）計算
- 資料結構如何被使用（陣列排序、圖形路徑權重）
本專案主要用於課堂展示與期末報告，不是實際與校方系統連線的真實後台。

---

## Tech Stack
請只使用：
- HTML
- CSS
- JavaScript
- 不使用後端
- 不使用資料庫
- 不使用登入系統
- 不使用 npm / React / Vue 等框架
最終作品必須可以直接用 `index.html` 開啟。

## File Structure
請維持以下檔案結構：
├── index.html
├── style.css
├── script.js
├── data/ # 任何其他資料都放置於此
│   ├── restaurantData.js # 模擬學餐店家與餐點資料
│   └── campusGraph.js    # 模擬校園大樓與學餐間的步行時間權重 (Graph)
├── AGENTS.md
├── PRD.md
├── USER_STORY.md
└── README.md

---

## Must Have
- 學餐餐廳卡片列表與即時等待時間標籤
- 依等待時間由短到長排序的功能
- 起始大樓選單與目標餐廳選單
- 「步行時間 + 排隊時間」總耗時動態計算器
- 使用 Array Sort 或 Graph 權重查詢的資料結構說明區塊
- 適合簡報展示、視覺醒目（如紅黃綠燈提示）的畫面

## Do Not Do
- 不要做太複雜的系統
- 不要加入真實 API
- 不要重寫全部程式
- 不要做需要安裝才能跑的功能

---

## 驗收標準 (重要)

### 1. 基本執行
- 使用者可以直接打開 `index.html` 看到完整畫面
- 不需要安裝額外套件
- 不需要登入
- 不需要後端伺服器
- 不需要 API key
- 頁面載入後不能出現明顯錯誤畫面
- 瀏覽器 console 不應該會有影響功能的 JavaScript error

### 2. Demo 流程
使用者必須能完成一個完整展示流程：
1. 進入首頁，看見所有學餐卡片與等待時間。
2. 點擊「依時間排序」按鈕，餐廳卡片依序重新排列。
3. 在計算機區塊選擇「目前所在位置（大樓）」與「想吃的餐廳」。
4. 系統即時產生明確的「總耗時預估結果」與遲到警示顏色。
5. 結果可以被截圖或口頭說明。
6. 整個流程可以在 3 分鐘內展示完。
*如果是互動式作品，至少要有一條「保證可成功展示」的 demo path。

### 3. 核心功能
- 至少完成 MVP 中定義的核心功能（即時時間、排序、複合時間計算）。
- 核心功能不能只是靜態文字描述，必須有可操作或可觀察的結果。
- 每個按鈕或主要互動元件都應該有明確作用。
- 不應存在點了沒有反應、但看起來像主要功能的按鈕。
- 如果功能尚未完成，請不要放在主要畫面上誤導使用者。

完成時必須能回答：
- 使用了哪一種資料結構？（例如：Array 儲存餐廳、Graph/Matrix 儲存步行權重）
- 資料是怎麼存的？（使用靜態 Mock Data JavaScript 物件/陣列）
- 為什麼這裡適合用這種資料結構？
- 這個資料結構如何影響功能運作？

### 4. 檔案與程式碼
- 主要檔案命名清楚，例如 `index.html`、`style.css`、`script.js`。
- 不要把大量 JavaScript 全部塞在 HTML 裡。
- 不要留下大量沒用到的程式碼。
- 不要留下測試用假按鈕、假頁面或無意義文字。
- 函式名稱應該能看出用途。
- 如果有資料檔，資料結構要清楚，不要全部寫成難以閱讀的一大坨字串。

### 5. 穩定性
- 重新整理頁面後仍可以正常使用。
- 操作順序稍微不同時，不應直接壞掉。
- 沒有選資料（如未選大樓或餐廳）就按按鈕時，系統應該給出提示，而不是報錯。
- 如果輸入錯誤，畫面應該有清楚回饋。
- Demo 前應至少完整跑過 3 次主要流程。

### 6. 範圍控制
以下內容不是必要項目，不應該影響 MVP 完成：
- 登入系統
- 後端資料庫
- 真實金流
- 真實通知系統
- 真實 GPS
- 複雜 AI 模型
- 大量頁面
- 過度精緻但無法操作的動畫

### 10. 最終完成定義
當以下條件都達成時，才算完成：
- 可以正常開啟作品。
- 可以完成主要 demo 流程。
- 有清楚的輸入、處理、輸出。
- 至少一個資料結構有被實際應用在功能中。
- 畫面適合展示。
- 團隊能說明作品邏輯。
- 沒有明顯會讓 demo 中斷的錯誤。

補充：以上驗收過程建議使用 browser use 功能 (/browser) 做測試。
